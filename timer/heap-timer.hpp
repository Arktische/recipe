#ifndef HEAP_TIMER_HPP_
#define HEAP_TIMER_HPP_
#include <heap>
namespace cpputil {
    class HeapTimer {
    public:
        HeapTimer(int delay)
    protected:
        time_t expire;
        void* data;

    };
}
#endif